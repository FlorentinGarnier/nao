<div>
    <ul>
        {% for category in categories %}
            <li><a href="" title="">{{ category.name }}</a></li>
        {% endfor %}
        <li><a href="{{ path('add') }}" title="">Créer un nouveau post</a></li>
    </ul>
    <input type="text" placeholder="Recherche dans cette catégorie">
</div>
